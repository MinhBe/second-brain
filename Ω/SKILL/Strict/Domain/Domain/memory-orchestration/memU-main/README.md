# memU-main

This directory is a source workspace/framework for memory management, not a directly activatable skill package.

It is kept in `memory-orchestration` because its subject belongs there, but it should not be counted as an operational `SKILL.md` skill unless a wrapper skill is added later.
