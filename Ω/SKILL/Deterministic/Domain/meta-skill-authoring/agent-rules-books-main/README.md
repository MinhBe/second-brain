# agent-rules-books-main

This directory is a reference library of programming and agent-design books/rules, not a directly activatable skill package.

Use it as background material for skill or agent authoring. It should not be counted as an operational `SKILL.md` skill.
