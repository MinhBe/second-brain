// Phase 5: the session router must stop at the reroute ceiling. v1 has no router, so nothing to test yet.
export const name = 'reroute-ceiling-exceeded';
export const skip = 'phase-5: no session router in v1 (single profile per run by design)';
export async function run() {}
